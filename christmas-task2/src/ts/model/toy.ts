import {Shape, Size, Color} from './enums';

export type Toy = {
    num: number,
    name: string,
    count: number,
    year: number,
    shape: Shape,
    color: Color,
    size: Size,
    favorite: boolean

}